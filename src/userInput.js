class UserInput {
    constructor(station, trainType, direction) {
        this.station = station;
        this.trainType = trainType;
        this.direction = direction;
    }
}

module.exports = {
    UserInput
};